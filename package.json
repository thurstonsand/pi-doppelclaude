{
  "name": "pi-claude-bridge",
  "version": "0.6.2",
  "private": false,
  "description": "Pi extension that uses Claude Code (via Agent SDK) as a model provider.",
  "keywords": [
    "pi-package",
    "pi",
    "extension",
    "claude-code",
    "claude-agent-sdk"
  ],
  "author": "Eli Dickinson",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/elidickinson/pi-claude-bridge.git"
  },
  "engines": {
    "node": ">=20"
  },
  "files": [
    "src",
    "README.md",
    "LICENSE",
    "assets"
  ],
  "scripts": {
    "lint": "npm run typecheck && npm run lint:shell",
    "lint:shell": "shellcheck -x -P SCRIPTDIR tests/*.sh tests/lib/*.sh",
    "test:unit": "node --import tsx --test tests/unit-*.ts",
    "test": "set -a && [ -f .env.test ] && . .env.test; set +a && npm run test:unit && tests/int-smoke.sh && tests/int-settings.sh && tests/int-multi-turn.sh && tests/int-cache.sh && node --import tsx --test tests/int-*.ts",
    "test:usage": "node --import tsx tests/usage-test.ts",
    "typecheck": "tsc --noEmit"
  },
  "type": "module",
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.219",
    "@anthropic-ai/sdk": "^0.114.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "cc-session-io": "^0.3.1",
    "change-case": "^5.4.4",
    "typebox": "^1.3.7",
    "zod": "^4.4.3"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": ">=0.81.1",
    "@earendil-works/pi-coding-agent": ">=0.81.1"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "^0.81.1",
    "@earendil-works/pi-coding-agent": "^0.81.1",
    "@types/node": "^26.1.1",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2"
  },
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  }
}
